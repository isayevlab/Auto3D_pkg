"""The presentation layer. Namespacing only -- this package re-exports nothing.

A module's directory is now its layer, so the layer map in
``tests/test_layer_boundaries.py`` is derived from these names rather
than declared beside them.
"""
